let tabButton = document.querySelectorAll(".tablinks .tab .tab-links");

let tabButtons2 = document.querySelectorAll(".wrapper");

let tabcontainers = document.querySelectorAll(".tablinks .tab");

// function tabs() {
//   document.querySelectorAll(".tablinks").forEach((tabButton) => {
//     tabButton.addEventListener("click", () => {
//       let tabBar = tabButton.parentElement;
//       let wrapper = tabBar.parentElement;
//       let tabsNumber = tabButton.dataset.forTab;
//       let tabToActivate = wrapper.querySelector(
//         `.conversion-div[data-tab=${tabsNumber}`
//       );
//       tabBar.querySelectorAll(".tablinks").forEach(tabButton)

//     });
//   });
// }
// tabs();
